apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bluebank

resources:
- deployment.yaml
- service.yaml
- route.yaml

commonLabels:
  app: vllm-coding
  workload: swe-coding
  workload-class: internal

# Made with Bob
