apiVersion: v1
kind: Service
metadata:
  name: vllm-coding
  namespace: bluebank
  labels:
    app: vllm-coding
    workload: swe-coding
    workload-class: internal
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: vllm-coding

# Made with Bob
