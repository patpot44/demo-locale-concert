apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-coding
  namespace: bluebank
spec:
  template:
    spec:
      containers:
      - name: vllm
        resources:
          requests:
            nvidia.com/gpu: "1"
          limits:
            nvidia.com/gpu: "1"

# Made with Bob
