apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-coding
  namespace: bluebank
spec:
  template:
    spec:
      containers:
      - name: vllm
        resources:
          requests:
            nvidia.com/mig-2g.20gb: "1"
          limits:
            nvidia.com/mig-2g.20gb: "1"

# Made with Bob
